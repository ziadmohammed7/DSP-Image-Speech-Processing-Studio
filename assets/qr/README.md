# QR Assets

This folder stores QR-code assets related to application download or release access.

Current asset:

- `application-download-qr.png`

If the download link changes, regenerate the QR code and replace the image with the same filename so README links remain stable.
